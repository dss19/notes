import React, { Component } from 'react';

class NotesCreate extends Component {
  render() {
    return (
      <button type="submit" className="notes-create">create note</button> 
    );
  }
}

export default NotesCreate;